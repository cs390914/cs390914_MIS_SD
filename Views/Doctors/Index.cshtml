@model IEnumerable<cs390914_MIS_SD.Models.Doctors>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AreaOfPractice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoctorSince)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoctorPhone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AreaOfPractice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoctorSince)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoctorPhone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DoctorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DoctorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DoctorID })
        </td>
    </tr>
}

</table>
